import { ComponentProps } from "@stitches/react";
import { styled } from "../../stitches.config";

export const SelectStyles = styled("div", {});

export type SelectVariants = ComponentProps<typeof SelectStyles>;
